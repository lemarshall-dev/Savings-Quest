# Savings Quest

Upload all files to the root of a public GitHub repository, then enable GitHub Pages from Settings → Pages → Deploy from a branch → `main` / root. Open the published URL in Safari and choose Share → Add to Home Screen.

The app stores goals and transactions in browser local storage. Use Back up before clearing browser data or changing phones.
